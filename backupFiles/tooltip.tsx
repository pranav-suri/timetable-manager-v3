/* <Fade in={false}>
      <Tooltip title="Syncing data with the server...">
        <Box
          sx={{
            position: "fixed",
            bottom: 24,
            right: drawerState ? `calc(${drawerwidth}px + 24px)` : 24,
            zIndex: (theme) => theme.zIndex.tooltip + 1,
            backgroundColor: "rgba(0, 0, 0, 0.7)",
            color: "white",
            borderRadius: "50%",
            display: "flex",
            alignItems: "center",
            justifyContent: "center",
            p: 1,
            transition: "right 0.2s ease-in-out",
          }}
        >
          <CircularProgress size={24} color="inherit" />
        </Box>
      </Tooltip>
    </Fade> */
